<div class="block dashboard-blocks {{$class}} {{$color}}">
    <div class="icon">
        <i class="fa fa-users"></i><br>
        <a class="dashboard-links" href="{{url($url)}}" >{{$text}}</a>
        <p>({{$count}})</p>
    </div>

</div>